# Copy to .env on NAS. Do not commit .env.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=data_solution
POSTGRES_HOST_PORT=5432

# Repo root (for schema init mount)
DATA_SOLUTION_ROOT=../../
